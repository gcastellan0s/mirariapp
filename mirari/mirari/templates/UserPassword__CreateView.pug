extends "generic/CreateView.html"
-load static
-load crispy_forms_tags

block styles
    {{block.super}}

block content
    .row
        .col-xl-4.offset-xl-4
            .kt-login__body.mt-5
                form#kt_form.kt-form.kt-form--fit.kt-form--label-right.form-horizontal(method="post")
                    .kt-login__form
                        {%if object.needChangePassword and object.pk == request.user.pk%}
                        .kt-login__title
                            h5 DEBES CAMBIAR TU CONTRASEÑA PARA CONTINUAR
                        {%endif%}
                        {% crispy form %}
                        .kt-login__actions
                            button#kt_login_signin_submit.btn.btn-brand.btn-elevate.kt-login__btn-primary.btn-block(type='submit') CAMBIAR

block js
    {{block.super}}
                    
script.
    {%block data%} 
        {{block.super}}
    {%endblock data%}

    {%block methods%}
        {{block.super}}
    {%endblock methods%}